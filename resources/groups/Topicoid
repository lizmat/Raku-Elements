given
syntax topic
topicalize argument for scope

when
syntax
if smartmatched topicalize argument for scope

andthen topic
infix thunky
topicalize left for right thunk

orelse topic
infix thunky
low precedence if defined OR, topicalizing left for right thunk


contains the functions that topicalize (set C<$_>) in some situations.
