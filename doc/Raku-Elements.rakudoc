=begin pod

=head1 NAME

Raku::Elements - The Elements of the Raku Programming Language

=head1 SYNOPSIS

=begin code :lang<raku>
use Raku::Elements;

my $info = Raku::Elements.new;
=end code

=head1 DESCRIPTION

The C<Raku::Elements> distribution attempts to provide a programmatical
interface to an alternate description of the Raku Programming Language,
inspired by the "Periodic Table of the Operators" by I<Mark Lentczner>.

It basically provides an (incomplete) overview of the features of the
Raku Programming Language, sorted into groups in a way similar to the
"Periodic Table of Elements".

Please note that this is still very much a work in progress: a lot
of explanatory texts still need to be added.  Pull requests are B<very>
welcome!

=head1 CLASSES

=head2 Raku::Elements

=begin code :lang<raku>
my $info = Raku::Elements.new;
say "Groups: $info.groups.elems()";
say "Tags: $info.tags.elems()";
=end code

The programmatical interface to the elements of the Raku Programming
Language.  Can be instantiated by calling C<.new>.  Provides these
methods:

=head3 elements

A C<Map> with the elements of the Raku Programming Language.  The key
represents either the name of an C<Raku::Element>, or one of its
alternates.  The value is a C<List> of one or more C<Raku::Element>
objects that share the same name (but may have different tags).

=head3 groups

A C<Map> with C<Raku::Group> objects, keyed to their name.

=head3 tags

A C<Map> with C<Raku::Tag> objects, keyed to their name.

=head2 Raku::Group

The information about an element group of the Raku Programming Language.
It is usually created automatically when a C<Raku::Elements> object is
instantiated.

=head3 name

The name of the element group.

=head3 description

The description of the element group (if any)

=head3 elements

A list of C<Raku::Element> objects belonging to this group.

=head2 Raku::Element

=head3 name

The name of the element.  This may be non-alphabetic, specifically in
the case of operators such as C<+>.

=head3 alternates

Alternate names of the element, for instance an Unicode version versus
an ASCII version of an operator, such as C<∈> as al alternate name for
C<(elem)>.  Usually empty.

=head3 tags

A list of tags that apply to this element, e.g. C<infix> for an infix
operator.  Has at least one element.

=head3 tagline

The tagline of the element: a one-line description.

=head3 url

A URL for more information about this element.

=head3 description

A more general description of the element.  Optional.

=head2 Raku::Tag

The information about an element tag of the Raku Programming Language.
It is usually created automatically when a C<Raku::Elements> object is
instantiated.

=head3 name

The name of the tag.

=head3 description

The description of the tag (if any)

=head3 elements

A list of C<Raku::Element> objects belonging that have this tag set.

=head1 ELEMENTS

This section of the documentation is generated from the information
supplied by the C<Raku::Elements> object.  It is provided here as an
example of what can be done with the information provided by this
distribution.

=comment PLEASE DON'T CHANGE ANYTHING BELOW THIS LINE
=comment Generated on 2025-11-15T20:29:04+01:00 by ./make-rakudoc.raku

=head2 Raku Element Tags

L<C<boolean>|#boolean>
L<C<circumfix>|#circumfix>
L<C<constant>|#constant>
L<C<dynamic>|#dynamic>
L<C<hash>|#hash>
L<C<infix>|#infix>
L<C<integer>|#integer>
L<C<interrupt>|#interrupt>
L<C<junction>|#junction>
L<C<list>|#list>
L<C<macro>|#macro>
L<C<method>|#method>
L<C<modifier>|#modifier>
L<C<numeric>|#numeric>
L<C<order>|#order>
L<C<pair>|#pair>
L<C<postfix>|#postfix>
L<C<prefix>|#prefix>
L<C<quanthash>|#quanthash>
L<C<range>|#range>
L<C<regex>|#regex>
L<C<sequence>|#sequence>
L<C<statement>|#statement>
L<C<string>|#string>
L<C<sub>|#sub>
L<C<syntax>|#syntax>
L<C<term>|#term>
L<C<thunky>|#thunky>
L<C<topic>|#topic>
L<C<variable>|#variable>

=head3 boolean

Produces a C<Bool> value: either C<True> or C<False>.

=head3 circumfix

Operators that consist of an opening and a closing tag, with their
arguments between them.

=head3 constant

Refers to a constant defined by the Raku Programming Language.

=head3 dynamic

Variable lookups happen in the dynamic scope at execution (the current
callstack) rather than lookups at compile time.  Returns a C<Failure>
if the variable could not be found.

=head3 hash

Produces a C<Hash> object.

=head3 infix

Operators that typically take two values, one to the left and one to
the right of the operator.

=head3 integer

Produces an C<Int> value.

=head3 interrupt

Interrupts the normal flow of execution.

=head3 junction

Produces a C<Junction> object.

=head3 list

Produces a C<List> or C<Array> object.

=head3 macro

Syntax structures that may change the semantics of the code given
at compile time.

=head3 method

A C<Callable> with a name that takes an invocant as its first argument,
and which is typically part of a C<class>, C<role> or C<grammar>.

=head3 modifier

A syntax structure at the end of a statement that affects whether
the statement will actually be executed.

=head3 numeric

Produces a C<Numeric> value.

=head3 order

Produces an C<Order> value, as in C<Less>, C<Same> or C<More>.

=head3 pair

Produces a C<Pair> object.

=head3 postfix

An operator that is placed B<after> the argument it operates on.

=head3 prefix

An operator that is placed B<before> the argument it operates on.

=head3 quanthash

May produce a C<QuantHash> object (which is one of:
C<Set>, C<SetHash>, C<Bag>, C<BagHash>, C<Mix>, C<MixHash>), or
uses C<QuantHash> semantics to return a C<Bool>.

=head3 range

Produces a C<Range> object.

=head3 regex

Related to Raku regexes (aka "regular expressions").

=head3 sequence

Produces a lazy C<Seq> (sequence) object.

=head3 statement

A syntax structure at the start of a statement.

=head3 string

Produces a C<Str> object.

=head3 sub

A C<Callable> with a name that can be returned from.

=head3 syntax

A syntax element of the Raku Programming Language that typically
doesn't have its own (visible) runtime component.

=head3 term

Elements of the Raku Programming Language that exists on their own and
which are known to not accept any arguments.

=head3 thunky

Having special syntax handling causing it to not immediately be executed
(like a C<Callable>) but without having an independent scope.

=head3 topic

Either sets the topic variable (C<$_>), or uses the topic variable as
a default argument.

=head3 variable

Refers to a variable defined by the Raku Programming Language.

=head2 Raku Element Groups

L<C<Addenoid>|#Addenoid>
L<C<Bindoid>|#Bindoid>
L<C<Buildoid>|#Buildoid>
L<C<Condoid>|#Condoid>
L<C<Constoid>|#Constoid>
L<C<Declaroid>|#Declaroid>
L<C<Differentoid>|#Differentoid>
L<C<Environoid>|#Environoid>
L<C<Equalish>|#Equalish>
L<C<Expansive>|#Expansive>
L<C<Feedoid>|#Feedoid>
L<C<Flippant>|#Flippant>
L<C<Hyperoid>|#Hyperoid>
L<C<IOoid>|#IOoid>
L<C<Incremental>|#Incremental>
L<C<Introspectoid>|#Introspectoid>
L<C<Junctive>|#Junctive>
L<C<Lexicoid>|#Lexicoid>
L<C<Mathematicals>|#Mathematicals>
L<C<Metaoid>|#Metaoid>
L<C<Methodic>|#Methodic>
L<C<Mixoid>|#Mixoid>
L<C<Modifoid>|#Modifoid>
L<C<Multiplicoid>|#Multiplicoid>
L<C<Normaloid>|#Normaloid>
L<C<Orderoid>|#Orderoid>
L<C<Quantoid>|#Quantoid>
L<C<Rangoid>|#Rangoid>
L<C<Reductoid>|#Reductoid>
L<C<Replicant>|#Replicant>
L<C<Sequoid>|#Sequoid>
L<C<Shortoid>|#Shortoid>
L<C<Stuboid>|#Stuboid>
L<C<Talkoid>|#Talkoid>
L<C<Termoid>|#Termoid>
L<C<Throwoid>|#Throwoid>
L<C<Topicoid>|#Topicoid>
L<C<Tweakoid>|#Tweakoid>

=head2 Addenoid

The Addenoid group contains all infix operators that could be considered doing addition
or substraction functions.

=head3 L<C«+» numeric add|https://docs.raku.org/language/operators#infix_+>
=item L<infix|#infix> L<numeric|#numeric>

Coerces both sides to a C<Numeric> value, and then adds them.

=head3 L<C«-» numeric subtract|https://docs.raku.org/language/operators#infix_->
=item alternates: C«−»
=item L<infix|#infix> L<numeric|#numeric>

Coerces both sides to a C<Numeric> value, and then substracts the right
side from the left side.

=head3 L<C«~» string concatenation|https://docs.raku.org/language/operators#infix_~>
=item L<infix|#infix> L<string|#string>

Coerces both sides to a C<Str>, and then concatenates them.

=head3 L<C«+&» integer AND|https://docs.raku.org/language/operators#infix_+&>
=item L<infix|#infix> L<integer|#integer>

Coerces both sides to an C<Int> value, and then does a bitwise AND.

=head3 L<C«+|» integer OR|https://docs.raku.org/language/operators#infix_+|>
=item L<infix|#infix> L<integer|#integer>

Coerces both sides to an C<Int> value, and then does a bitwise OR.

=head3 L<C«+^» integer XOR|https://docs.raku.org/language/operators#infix_+^>
=item L<infix|#infix> L<integer|#integer>

Coerces both sides to an C<Int> value, and then does a bitwise XOR.

=head3 L<C«~&» string AND|https://docs.raku.org/language/operators#infix_~&>
=item L<infix|#infix> L<string|#string>

Coerces both sides to a C<Buffer> value, and then does a bitwise AND
and converts the result back to a C<Str>.

=head3 L<C«~|» string OR|https://docs.raku.org/language/operators#infix_~|>
=item L<infix|#infix> L<string|#string>

Coerces both sides to a C<Buffer> value, and then does a bitwise OR
and converts the result back to a C<Str>.

=head3 L<C«~^» string XOR|https://docs.raku.org/language/operators#infix_~^>
=item L<infix|#infix> L<string|#string>

Coerces both sides to a C<Buffer> value, and then does a bitwise XOR
and converts the result back to a C<Str>.

=head3 L<C«?&» boolean AND|https://docs.raku.org/language/operators#infix_?&>
=item L<infix|#infix> L<boolean|#boolean>

Coerces both sides to a C<Bool> value, and then does a logical AND.

=head3 L<C«?|» boolean OR|https://docs.raku.org/language/operators#infix_?|>
=item L<infix|#infix> L<boolean|#boolean>

Coerces both sides to a C<Bool> value, and then does a logical OR.

=head3 L<C«?^» boolean XOR|https://docs.raku.org/language/operators#infix_?^>
=item L<infix|#infix> L<boolean|#boolean>

Coerces both sides to a C<Bool> value, and then does a logical XOR.

=head2 Bindoid

The Bindoid group contains all macro-ish infix operators that perform raw binding,
possibly into a container (which would make it an assignment).

=head3 L<C«=» assign value(s)|https://docs.raku.org/language/operators#infix_=_(item_assignment)>
=item L<infix|#infix> L<macro|#macro>

Performs an assignment.  The left side is supposed to be either a
C<Scalar>, a C<Positional> with C<Scalar> containers (typically an
C<Array> such as C<my @foo>), or an C<Associative> with C<Scalar>
containers (typically a C<Hash> such as C<my %bar>).
=begin code :lang<raku>
my $a   = 42;
my @foo = 1,2,3,4,5;
my %bar = a => 42, b => 666, c => 137;
=end code

=head3 L<C«:=» bind right value to left lexpad entry|https://docs.raku.org/language/operators#infix_:=>
=item alternates: C«≔»
=item L<infix|#infix> L<macro|#macro>

Performs a binding operation on the lexpad entry on the left.  This
is generally done to indicate that it is an immutable value, or if
you want to alias one container to another.
=begin code :lang<raku>
my $b := 42;  # immutable
my $c  = 666;
my $d := $c;  # alias
$d = 137;
say $c;  # 137
=end code

=head2 Buildoid

The Buildoid group contains all elements that convert a given set of arguments into one of
a C<Scalar>, C<Pair>, C<Positional> or C<Associative> object.

=head3 L<C«=>» C<Pair> constructor, named argument specification|https://docs.raku.org/language/operators#infix_=%3E>
=item L<infix|#infix> L<syntax|#syntax> L<pair|#pair>

Indicates a named argument inside a C<Capture> (for instance, as
argument to a subroutine call).  Otherwise it functions as a
C<Pair> constructor, with the left side being the key.
=begin code :lang<raku>
frobnicate(a => 42);  # named argument
my $p = foo => 42;    # Pair
=end code

=head3 L<C«,» C<List> constructor|https://docs.raku.org/language/operators#infix_,>
=item L<infix|#infix> L<list|#list>

Places all of its arguments in a C<List>.  Note that parentheses
are B<not> needed for a list to be created.  The only exception
is the empty C<List>, which can be constructed by C<()>.

=head3 L<C«[ ]» C<Array> constructor|https://docs.raku.org/language/operators#circumfix_[_]>
=item L<circumfix|#circumfix> L<list|#list>

Creates an C<Array> out of the given arguments, typically used
when assigning to a hash.
=begin code :lang<raku>
my %foo;
%foo<bar> = [1,2,3,4];
=end code

=head3 L<C«{ }» C<Block> or C<Hash> constructor|https://docs.raku.org/language/operators#term_{_}>
=item L<circumfix|#circumfix> L<syntax|#syntax> L<hash|#hash>

Either creates a C<Block> object (if it looks like there is Raku code
between the brackets), or a C<Hash> built from its arguments (which
is usually done when assigning to another C<Hash> or C<Array>)..
=begin code :lang<raku>
my &hello = { say "hello world" }                 # code, so Block
hello;  # hello world
my @menu = { salad => 4.50 }, { steak => 22.50 }  # args, so Hash
say @menu[0]<salad>;  # 4.5
=end code

=head3 L<C«:{ }» Object Hash constructor|https://docs.raku.org/language/hashmap#Non-string_keys_(object_hash)>
=item L<circumfix|#circumfix> L<hash|#hash>

=head3 L<C«' '» literal string constructor|https://docs.raku.org/language/101-basics#Double-quoted_strings_and_single-quoted_strings>
=item L<syntax|#syntax> L<string|#string>

=head3 L<C«" "» literal string constructor with interpolation|https://docs.raku.org/language/101-basics#Double-quoted_strings_and_single-quoted_strings>
=item L<syntax|#syntax> L<string|#string>

=head3 L<C«< >» literal word list constructor|https://docs.raku.org/language/operators#term_%3C_%3E>
=item L<syntax|#syntax> L<list|#list> L<string|#string>

=head3 L<C«<< >>» literal word list constructor with interpolation|https://docs.raku.org/language/quoting#Word_quoting_with_interpolation_and_quote_protection:_«_»>
=item alternates: C<« »>
=item L<syntax|#syntax> L<list|#list> L<string|#string>

=head3 C«$( )» turn argument(s) into an item
=item L<syntax|#syntax>

=head3 L<C«item» turn argument(s) into an item|https://docs.raku.org/type/Mu#routine_item>
=item L<sub|#sub> L<method|#method>

=head3 C«@( )» turn argument(s) into a list
=item L<syntax|#syntax> L<list|#list>

=head3 L<C«list» turn argument(s) into a list|https://docs.raku.org/type/List#routine_list>
=item L<sub|#sub> L<method|#method> L<list|#list>

=head3 C«%( )» turn argument(s) into a hash
=item L<syntax|#syntax> L<hash|#hash>

=head3 L<C«hash» turn argument(s) into a hash|https://docs.raku.org/type/Any#method_hash>
=item L<sub|#sub> L<method|#method> L<hash|#hash>

=head2 Condoid

The Condoid group contains the statement syntaxes to indicate conditional execution
of code.

=head3 L<C«?? !!» ternary logic|https://docs.raku.org/language/operators#infix_??_!!>
=item L<syntax|#syntax>

Depending on the condition (before the C<??>) produces the
value of the expression between C<??> and C<!!> (if true)
or the expression after C<!!> if the condition was B<not>
true.

=head3 L<C«if» execute block if condition is true|https://docs.raku.org/language/control#if>
=item L<syntax|#syntax> L<statement|#statement>

First of one or more blocks of conditional execution.  Will
execute the block if the condition was true.

=head3 L<C«elsif» execute block if condition is true and previous false|https://docs.raku.org/language/control#else/elsif>
=item L<syntax|#syntax> L<statement|#statement>

Followup of C<if> or another C<elsif>, checked if the first
(or previous) condition evaluated to false.  Executes the
associated block if true.

=head3 L<C«else» execute block if none of previous blocks were executed|https://docs.raku.org/language/control#else/elsif>
=item L<syntax|#syntax> L<statement|#statement>

Final followup of C<if>, C<elsif>, C<with> or C<orwith>.  Executes
the associated block if none of the previous blocks were executed.

=head3 L<C«unless» execute block if condition is false|https://docs.raku.org/language/control#unless>
=item L<syntax|#syntax> L<statement|#statement>

A single block of conditional execution, to be executed if the
given condition evaluates to false.

=head3 L<C«with» execute block if value is defined, and set topic|https://docs.raku.org/language/control#with_orwith_without>
=item L<syntax|#syntax> L<statement|#statement> L<topic|#topic>

Start of one or more blocks of conditional execution.  Depending
on whether the expression evaluates to a defined value, the block
will be executed with the topic (C<$_>) set to the value inside
the block.

=head3 L<C«orwith» execute block if value is defined and previous not, and set topic|https://docs.raku.org/language/control#with_orwith_without>
=item L<syntax|#syntax> L<statement|#statement>

Followup of C<with> or another C<orwith>, checked if the first
(or previous) expressions did not produce a defined value.
Depending on whether the expression evaluates to a defined value,
the block will be executed with the topic (C<$_>) set to the value
inside the block.

=head3 L<C«without» execute block if value is not defined, and set topic|https://docs.raku.org/language/control#with_orwith_without>
=item L<syntax|#syntax> L<statement|#statement>

A single block of conditional execution, to be executed if the
given expression evaluates to undefined value.  Sets the topic
(C<$_>) with the value inside the block.

=head3 L<C«once» execute given block / thunk only once|https://docs.raku.org/language/control#once>
=item L<syntax|#syntax> L<statement|#statement> L<thunky|#thunky>

Execute the given block once int the given scope.  The code:
=begin code :lang<raku>
once say "foo";
=end code
is equivalent to:
=begin code :lang<raku>
state $flag;
say "foo" unless $flag++;
=end code

=head2 Constoid

The Constoid group contains all constant that are always available for usage.

=head3 L<C«$?PACKAGE» the current package|https://docs.raku.org/language/variables#index-entry-$%3FPACKAGE>
=item L<constant|#constant>

The type object for the current package (as created by C<package>,
C<module>, C<class>, C<role> or C<grammar>).

=head3 L<C«$?MODULE» the current module|https://docs.raku.org/language/variables#index-entry-$?MODULE>
=item alternates: C«::?MODULE»
=item L<constant|#constant>

The type object for the current module.  Available only if within
a C<module> scope.

=head3 L<C«$?CLASS» the current class / grammar|https://docs.raku.org/language/variables#index-entry-$%3FCLASS>
=item alternates: C«::?CLASS»
=item L<constant|#constant>

The type object for the current class.  Available only if within
a C<class> or C<grammar> scope, or

=head3 L<C«$?ROLE» the current role|https://docs.raku.org/language/variables#index-entry-$%3FROLE>
=item alternates: C«::?ROLE»
=item L<constant|#constant>

The type object for the current role.  Available only if within
a C<role> scope.

=head3 L<C«$?TABSTOP» number of spaces in a TAB|https://docs.raku.org/language/variables#index-entry-$%3FTABSTOP>
=item L<constant|#constant>

The number of spaces for a TAB character.

=head3 L<C«$?NL» the newline character(s)|https://docs.raku.org/language/variables#index-entry-$%3FNL>
=item L<constant|#constant>

What a newline C<\n> means.  Can be set with the C<newline>
pragma.

=head3 L<C«$?BITS» default number of bits for native integers|https://docs.raku.org/language/variables#Rakudo-specific_compile-time_variables>
=item L<constant|#constant>

The number of bits for native integers, usually 64.

=head3 L<C«&?ROUTINE» the current Routine (sub / method / submethod)|https://docs.raku.org/language/variables#&?ROUTINE>
=item L<constant|#constant> L<sub|#sub> L<method|#method>

The C<Routine> object of the current C<sub>, C<method> or C<submethod>.

=head3 L<C«&?BLOCK» the current block|https://docs.raku.org/language/variables#&?BLOCK>
=item L<constant|#constant>

The C<Block> object associated with the current block (code between
curly braces).

=head3 L<C«$?DISTRIBUTION» distribution info|https://docs.raku.org/language/variables#$?DISTRIBUTION>
=item L<constant|#constant>

The (possibly auto-generated) object that performs the C<Distribution>
role, expected to contain information usually found in the META6.json
file of a distribution.  Only available if the compilation unit is
executed as a module, not as a program.  Otherwise C<Nil>.

=head2 Declaroid

The Declaroid group contains the syntax for indicating the scope of an element to be defined.

=head3 L<C«my» define something in C<MY::> scope|https://docs.raku.org/language/variables#The_my_declarator>
=item L<syntax|#syntax>

Define an element in the current block's C<MY::> stash.

=head3 L<C«our» define something in C<OUR::> scope|https://docs.raku.org/language/variables#The_our_declarator>
=item L<syntax|#syntax>

Define an element in the current compilation unit's C<OUR::> stash.

=head3 L<C«anon» define something without storing it in a scope|https://docs.raku.org/language/variables#The_anon_declarator>
=item L<syntax|#syntax>

Define an element B<without> storing it in any scope.

=head3 L<C«state» define something in C<MY::> scope, retain value|https://docs.raku.org/language/variables#The_state_declarator>
=item L<syntax|#syntax>

Define an element in the current block's C<MY::> stash, but
keep it alive between incantations of the block (and only run the
initializer on initial execution).

=head2 Differentoid

The Differentoid group contains the infix operators that return True if the compared elements
are different in a way, and False if they are considered equal in that
way.

=head3 C«before» generic less
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«<» numerically less
=item L<infix|#infix> L<numeric|#numeric> L<boolean|#boolean>

=head3 C«lt» alphabetically less
=item L<infix|#infix> L<string|#string> L<boolean|#boolean>

=head3 C«<=» numerically less or equal
=item alternates: C«≤»
=item L<infix|#infix> L<numeric|#numeric> L<boolean|#boolean>

=head3 C«le» alphabetically less or equal
=item L<infix|#infix> L<string|#string> L<boolean|#boolean>

=head3 C«!=» numeric inequality
=item alternates: C«≠»
=item L<infix|#infix> L<numeric|#numeric> L<boolean|#boolean>

=head3 C«ne» string inequality
=item L<infix|#infix> L<string|#string> L<boolean|#boolean>

=head3 C«>=» numerically more or equal
=item L<infix|#infix> L<numeric|#numeric> L<boolean|#boolean>

=head3 C«ge» alphabetically more or equal
=item L<infix|#infix> L<string|#string> L<boolean|#boolean>

=head3 C«>» numerically more
=item L<infix|#infix> L<numeric|#numeric> L<boolean|#boolean>

=head3 C«gt» alphabetically more
=item L<infix|#infix> L<string|#string> L<boolean|#boolean>

=head3 C«after» generic more
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«!(elem)» is not element in
=item alternates: C«∉»
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«!(cont)» does not contain element
=item alternates: C«∌»
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«!(<)» is not a strict quanthash subset
=item alternates: C«⊄»
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«!(<=)» is not a quanthash subset
=item alternates: C«⊈»
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«!(==)» quanthash inequality
=item alternates: C«≢»
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«!(>=)» is not a quanthash superset
=item alternates: C«⊉»
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«!(>)» is not a strict quanthash superset
=item alternates: C«⊅»
=item L<infix|#infix> L<boolean|#boolean>

=head2 Environoid

The Environoid group contains all dynamic variables related to the actual hardware
/ software the current process is running under.

=head3 L<C«%*ENV» access to operating system environment variables|https://docs.raku.org/language/variables#%*ENV>
=item L<variable|#variable> L<dynamic|#dynamic> L<hash|#hash>

A hash that contains all of the environment variables of the operating
system when Raku is started.  May be altered.  Serves as a default for
environment variables for any forked process.

=head3 L<C«$*PID» the Process IDentifier|https://docs.raku.org/language/variables#$*PID>
=item L<variable|#variable> L<dynamic|#dynamic> L<integer|#integer>

An integer value for the PID of the current process.

=head3 L<C«$*USER» the current user|https://docs.raku.org/language/variables#$*USER>
=item L<variable|#variable> L<dynamic|#dynamic> L<integer|#integer> L<string|#string>

An C<IntStr> with information about the current user (uid and name).

=head3 L<C«$*GROUP» the group of the current user|https://docs.raku.org/language/variables#$*GROUP>
=item L<variable|#variable> L<dynamic|#dynamic> L<integer|#integer> L<string|#string>

An C<IntStr> with group information about the current user (gid and
name) if supported by the operating system.

=head3 L<C«$*DISTRO» operating system information|https://docs.raku.org/language/variables#$*DISTRO>
=item L<variable|#variable> L<dynamic|#dynamic>

A C<Distro> object containing the information about the operating
system under which this process is running.

=head3 L<C«$*KERNEL» kernel information|https://docs.raku.org/language/variables#$*KERNEL>
=item L<variable|#variable> L<dynamic|#dynamic>

A C<Kernel> object containing the information about the kernel
under which this process is running.

=head3 L<C«$*HOMEDRIVE» home drive of user|https://docs.raku.org/language/variables#$*HOMEDRIVE>
=item L<variable|#variable> L<dynamic|#dynamic>

Windows only: an C<IO::Path> object of the home drive of the user,
based on the C<HOMEDRIVE> environment variable.

=head3 L<C«$*HOMEPATH» home path of user|https://docs.raku.org/language/variables#$*HOMEPATH>
=item L<variable|#variable> L<dynamic|#dynamic>

Windows only: an C<IO::Path> object of the home path of the user,
based on the C<HOMEPATH> environment variable.

=head3 L<C«$*HOME» generic home directory|https://docs.raku.org/language/variables#$*HOME>
=item L<variable|#variable> L<dynamic|#dynamic>

An C<IO::Path> object of the home directory of the user, based on
the C<HOME>, C<HOMEDRIVE> and C<HOMEPATH> environment variable
as appropriate for the operating system in use.

=head3 L<C«$*TMPDIR» directory for temporary files|https://docs.raku.org/language/variables#$*TMPDIR>
=item L<variable|#variable> L<dynamic|#dynamic>

An C<IO::Path> object that can be used to store temporary files.

=head3 L<C«$*SPEC» OS specific path semantics|https://docs.raku.org/language/variables#$*SPEC>
=item L<variable|#variable> L<dynamic|#dynamic>

An C<IO::Spec> subclass object containing any specific path semantics
of the operating system.

=head2 Equalish

The Equalish group contains the infix operators that return C<True> if the compared elements
are considered equal in a way, and C<False> if they are considered different
in that way.

=head3 L<C«~~» smart match|https://docs.raku.org/language/operators#infix_~~>
=item L<infix|#infix>

Performs a smart-match on the given arguments.  Technically, this is
calling the C<.ACCEPTS> method on the right side, giving it the
left side as the positional argument.  Which usually results in a
C<Bool> value.

=head3 L<C«eqv» canonical equivalence|https://docs.raku.org/language/operators#infix_eqv>
=item L<infix|#infix> L<boolean|#boolean>

Performs a deep equivalence check logically dependent on the C<.raku>
representation.

=head3 L<C«eq» string equality|https://docs.raku.org/language/operators#infix_eq>
=item L<infix|#infix> L<string|#string> L<boolean|#boolean>

Coerces both sides to a string and returns C<True> if the resulting
strings are equal.  Else C<False>.

=head3 L<C«==» numeric equality|https://docs.raku.org/language/operators#infix_>
=item alternates: C«⩵»
=item L<infix|#infix> L<numeric|#numeric> L<boolean|#boolean>

Coerces both sides to a C<Numeric> value, and then returns C<True>
if they are the same.  Else C<False>.

=head3 L<C«=~=» numeric almost equal|https://docs.raku.org/language/operators#infix_=~=>
=item alternates: C«≅»
=item L<infix|#infix> L<numeric|#numeric> L<boolean|#boolean>

Coerces both sides to a C<Numeric> value, and then returns C<True>
if they are the same within C<$*TOLERANCE> (defaults to C<1e-15>).
Else C<False>.

=head3 L<C«===» value identity|https://docs.raku.org/language/operators#infix_===>
=item alternates: C«⩶»
=item L<infix|#infix> L<boolean|#boolean>

Decontainerizes both sides, and then returns C<True> if both sides
are the same object.  Else C<False>.

=head3 L<C«=:=» value identity without decontainerization|https://docs.raku.org/language/operators#infix_=:=>
=item L<infix|#infix> L<boolean|#boolean>

Returns C<True> if both sides are the same object B<without>
decontainerization.  Else C<False>.

=head3 L<C«(elem)» is element in|https://docs.raku.org/language/operators#infix_(elem),_infix_∈>
=item alternates: C«∈»
=item L<infix|#infix> L<boolean|#boolean> L<quanthash|#quanthash>

Returns C<True> if all values on the left side are part of the
right side, using C<QuantHash> semantics.  Else C<False>.

=head3 L<C«(cont)» contains element|https://docs.raku.org/language/operators#infix_(cont),_infix_∋>
=item alternates: C«∋»
=item L<infix|#infix> L<boolean|#boolean> L<quanthash|#quanthash>

Returns C<True> if all values on the right side are part of the
left side, using C<QuantHash> semantics.  Else C<False>.

=head3 L<C«(<)» is strict subset|https://docs.raku.org/language/operators#infix_(%3C),_infix_⊂>
=item alternates: C«⊂»
=item L<infix|#infix> L<boolean|#boolean> L<quanthash|#quanthash>

Returns C<True> if all values on the left side are part of the
right side and the right side has more values, using C<QuantHash>
semantics.  Else C<False>.

=head3 L<C«(<=)» is subset|https://docs.raku.org/language/operators#infix_(%3C=),_infix_⊆>
=item alternates: C«⊆»
=item L<infix|#infix> L<boolean|#boolean> L<quanthash|#quanthash>

Returns C<True> if all values on the left side are part of the
right side, using C<QuantHash> semantics.  Else C<False>.

=head3 L<C«(==)» quanthash equality|https://docs.raku.org/language/operators#infix_(==),_infix_≡>
=item alternates: C«≡»
=item L<infix|#infix> L<boolean|#boolean> L<quanthash|#quanthash>

Returns C<True> if both sides contain the same valuies, using
C<QuantHash> semantics.  Else C<False>.

=head3 L<C«(>=)» is superset|https://docs.raku.org/language/operators#infix_(%3E=),_infix_⊇>
=item alternates: C«⊇»
=item L<infix|#infix> L<boolean|#boolean> L<quanthash|#quanthash>

Returns C<True> if all values on the right side are part of the
left side, using C<QuantHash> semantics.  Else C<False>.

=head3 L<C«(>)» is strict superset|https://docs.raku.org/language/operators#infix_(%3E),_infix_⊃>
=item alternates: C«⊃»
=item L<infix|#infix> L<boolean|#boolean> L<quanthash|#quanthash>

Returns C<True> if all values on the right side are part of the
left side and the left side has more values, using C<QuantHash>
semantics.  Else C<False>.

=head2 Expansive

The Expansive group contains the postfix operators that expand on numeric values.

=head3 L<C«i» numeric multiplication with the imaginary unit (√-1)|https://docs.raku.org/type/Complex#postfix_i>
=item L<postfix|#postfix> L<numeric|#numeric>

C<4i> is the same as C<0+4i>.  Needs grouping for variables C<($a)i>
because otherwise the C<i> will be considered part of the variable
name.

=head3 L<C«ⁿ» numeric literal integer exponentiation|https://docs.raku.org/routine/%2A%2A>
=item L<postfix|#postfix> L<numeric|#numeric>

So C<$a²> is the same as C<$a ** 2>, etc.  Any of C<⁰¹²³⁴⁵⁶⁷⁸⁹⁺¯¯> may
be used.

=head2 Feedoid

The Feedoid group contains the operators that provide an alternate syntax for creating
a sequence of operations, where the result of one such operation
become the argument(s) for the next operation.

=head3 L<C«==>» feed left to right|https://docs.raku.org/language/operators#infix_==%3E>
=item L<infix|#infix>

C«(1,2,3,4) ==> sum() ==> say()» as opposed to C<say sum (1,2,3,4)>
or C<(1,2,3,4).sum.say>.

=head3 L<C«<==» feed right to left|https://docs.raku.org/language/operators#infix_%3C==>
=item L<infix|#infix>

C«say() <== sum() <== (1,2,3,4)» as opposed to C<say sum (1,2,3,4)>
or C<(1,2,3,4).sum.say>.

=head2 Flippant

The Flippant group contains the operators that produce C<True> or C<False> depending on
multiple conditions.

=head3 L<C«ff» flip-flop inclusive|https://docs.raku.org/language/operators#infix_ff>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head3 L<C«^ff» flip-flop excluding start|https://docs.raku.org/language/operators#infix_^ff>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head3 L<C«ff^» flip-flop excluding end|https://docs.raku.org/language/operators#infix_ff^>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head3 L<C«^ff^» flip-flop exclusive|https://docs.raku.org/language/operators#infix_^ff^>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head3 L<C«fff» sed-like flip-flop inclusive|https://docs.raku.org/language/operators#infix_fff>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head3 L<C«^fff» sed-like flip-flop excluding start|https://docs.raku.org/language/operators#infix_^fff>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head3 L<C«fff^» sed-like flip-flop excluding end|https://docs.raku.org/language/operators#infix_fff^>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head3 L<C«^fff^» sed-like flip-flop exclusive|https://docs.raku.org/language/operators#infix_^fff^>
=item L<infix|#infix> L<macro|#macro> L<boolean|#boolean>

=head2 Hyperoid

The Hyperoid group contains the operators that take the name of an infix operator and
repeatedly perform that operator on the provided argument list(s).

=head3 L<C«>>op<<» produce operator results for equal lists|https://docs.raku.org/language/operators#Hyper_operators>
=item alternates: C<»op« >
=item L<infix|#infix> L<syntax|#syntax>

Repeat the operation on each of the associated elements of two
C<Iterable>s with the same number of elements.  Throws an
exception if they are not equal.

=head3 L<C«>>op>>» produce operator results, left side leading|https://docs.raku.org/language/operators#Hyper_operators>
=item alternates: C<»op»>
=item L<infix|#infix> L<syntax|#syntax>

Repeat the operation on each of the associated elements of two
C<Iterable>s for each element on the left side.  Missing values
on the right side will be repeated.

=head3 L<C«<<op<<» produce operator results, right side leading|https://docs.raku.org/language/operators#Hyper_operators>
=item alternates: C<«op«>
=item L<infix|#infix> L<syntax|#syntax>

Repeat the operation on each of the associated elements of two
C<Iterable>s for each element on the right side.  Missing values
on the left side will be repeated.

=head3 L<C«<<op>>» produce operator results, longest side leading|https://docs.raku.org/language/operators#Hyper_operators>
=item alternates: C<«op»>
=item L<infix|#infix> L<syntax|#syntax>

Repeat the operation on each of the associated elements of two
C<Iterable>s for each element on the side with the most elements.
 Missing values on the other side will be repeated.

=head3 L<C«Zop» produce operator result, shortest side leading|Default for `op` is `,`>
=item L<infix|#infix> L<syntax|#syntax>

=head3 L<C«Xop» produce operator result for combinations|Default for `op` is `,`>
=item L<infix|#infix> L<syntax|#syntax>

=head2 IOoid

The IOoid group contains the functions that are available for input / output of data.

=head3 C«get» Read a line from C<C<$*STDIN>> / C<C<$*ARGFILES>>
=item L<sub|#sub> L<method|#method> L<string|#string>

=head3 C«getc» Read a character from C<C<$*STDIN>> / C<C<$*ARGFILES>>
=item L<sub|#sub> L<method|#method> L<string|#string>

=head3 C«read» Read bytes from an C<IO::Handle>
=item L<method|#method>

=head3 C«write» Write bytes to an C<IO::Handle>
=item L<method|#method>

=head3 C«slurp» Read entire contents of C<IO::Path> / C<IO::Handle>
=item L<sub|#sub> L<method|#method>

=head3 C«spurt» Replace content of C<IO::Path> / C<IO::Handle> with argument
=item L<sub|#sub> L<method|#method>

=head2 Incremental

The Incremental group contains the infix operators for incrementing / decrementing (usually)
numeric values.

The atomic versions of C<--> and C<++> were implemented for 6.d.  They
can only be applied to atomic integers.

=head3 C«++» pre-increment by 1
=item L<prefix|#prefix>

=head3 C«⚛++» atomic pre-increment by 1
=item L<prefix|#prefix> L<numeric|#numeric>

=head3 C«--» pre-decrement by 1
=item L<prefix|#prefix>

=head3 C«⚛--» atomic pre-decrement by 1
=item L<prefix|#prefix> L<numeric|#numeric>

=head3 C«++» post-increment by 1
=item L<postfix|#postfix>

=head3 C«⚛++» atomic post-increment by 1
=item L<postfix|#postfix> L<numeric|#numeric>

=head3 C«--» post-decrement by 1
=item L<postfix|#postfix>

=head3 C«⚛--» atomic post-decrement by 1
=item L<postfix|#postfix> L<numeric|#numeric>

=head2 Introspectoid

The Introspectoid group contains all dynamic variables that respresent some type if internal
state of the runtime.  Even though they can be changed or shadowed,
it is usually a bad design decision to do so.

=head3 L<C«$*INIT-INSTANT» when Raku was started|https://docs.raku.org/language/variables#$*INIT-INSTANT>
=item L<constant|#constant> L<dynamic|#dynamic>

The C<Instant> the raku process was started.

=head3 L<C«$*PROGRAM-NAME» name of current Raku program|https://docs.raku.org/language/variables#$*PROGRAM-NAME>
=item L<variable|#variable> L<dynamic|#dynamic> L<string|#string>

A string presentation of the path of the currently executing Raku
program.

=head3 L<C«$*PROGRAM» current Raku program|https://docs.raku.org/language/variables#$*PROGRAM>
=item L<constant|#constant> L<dynamic|#dynamic>

An C<IO::Path> of the currently executing Raku program.  Available at
compile time, and can thus be used as part of a C<use lib> statement.
=begin code :lang<raku>
use lib $*PROGRAM.sibling("lib");
=end code

=head3 L<C«$*EXECUTABLE-NAME» basename of the currently running Raku runtime|https://docs.raku.org/language/variables#$*EXECUTABLE-NAME>
=item L<constant|#constant> L<dynamic|#dynamic> L<string|#string>

A string presentation of the basename of the currently executing
Raku program (usually "raku" or "rakudo").

=head3 L<C«$*EXECUTABLE» currently running Raku runtime|https://docs.raku.org/language/variables#$*EXECUTABLE>
=item L<constant|#constant> L<dynamic|#dynamic>

An C<IO::Path> of the currently running Raku executable (typically
"rakudo").  Can be used as the initial argument to e.g. C<run>.

=head3 L<C«$*REPO» start of repository chain|https://docs.raku.org/language/variables#$*REPO>
=item L<constant|#constant> L<dynamic|#dynamic>

The first of a chain of C<CompUnit::Repository> objects that are
responsible for registering modules and making them available for
usage.

=head3 L<C«$*RAKU» information on this Raku|https://docs.raku.org/language/variables#$*RAKU>
=item L<constant|#constant> L<dynamic|#dynamic>

A C<Raku> object that contains information about the currently
executing version of the Raku Programming Language.

=head3 L<C«$*THREAD» the current thread|https://docs.raku.org/language/variables#$*THREAD>
=item L<constant|#constant> L<dynamic|#dynamic>

The C<Thread> object describing the currently active thread.

=head3 L<C«$*SCHEDULER» the logic for scheduling the running of async work|https://docs.raku.org/language/variables#$*SCHEDULER>
=item L<variable|#variable> L<dynamic|#dynamic>

The C<Scheduler> object that handles all scheduling of async jobs.

=head3 L<C«$*SAMPLER» the current C<Telemetry> sampler|https://docs.raku.org/language/variables#$*SAMPLER>
=item L<variable|#variable> L<dynamic|#dynamic>

The sampler logic for making snapshots of the system state.  Only
available if the C<Telemetry> module has been loaded.

=head2 Junctive

The Junctive group contains the infix operators and the functions that produce C<Junction>
objects.

=head3 C«&» junctive all
=item L<infix|#infix> L<junction|#junction>

=head3 C«|» junctive any
=item L<infix|#infix> L<junction|#junction>

=head3 C«^» junctive one
=item L<infix|#infix> L<junction|#junction>

=head3 C«all» junctive all
=item L<sub|#sub> L<method|#method> L<junction|#junction>

=head3 C«any» junctive any
=item L<sub|#sub> L<method|#method> L<junction|#junction>

=head3 C«one» junctive one
=item L<sub|#sub> L<method|#method> L<junction|#junction>

=head3 C«none» junctive none
=item L<sub|#sub> L<method|#method> L<junction|#junction>

=head2 Lexicoid

The Lexicoid group contains all variables that are always available for usage in a
lexical scope.

=head3 L<C«$» anonymous state variable|https://docs.raku.org/language/variables#The_$_variable>
=item L<variable|#variable>

The anonymous state variable can be used without an explicit
C<state> declaration.  Each reference to C<$> within a lexical
scope is in effect a separate variable.

=head3 L<C«$_» the current topic|https://docs.raku.org/language/variables#The_$__variable>
=item L<variable|#variable> L<topic|#topic>

Every block has its own topic variable.  It can be set directly,
or is set automatically with statements such as C<given>, C<with>
and C<for>.

=head3 L<C«$/» the current regex match result|https://docs.raku.org/language/variables#The_$/_variable>
=item L<variable|#variable> L<regex|#regex>

Results of many actions related to regular expressions set
this variable.  Each C<Routine> has its own copy of it.  It
also provides C<Positional> functionality, so that C<$/[0]>
refers to the first positional capture (which can be shortened
to C<$0>).  And it also provides the C<Associative> functionality
so that that C<$/<foo>> refers to the named capture "foo"
(which can be shortened to C«$<foo>».

=head3 L<C«$!» the current error variable|https://docs.raku.org/language/variables#The_$!_variable>
=item L<variable|#variable>

Typically contains the last caught exception seen.  Each
C<Routine> has its own copy of it.

=head2 Mathematicals

The Mathematicals group contains the mathematical terms that are provided by the core.

=head3 C«pi» The number π (3.1415...)
=item alternates: C«π»
=item L<term|#term> L<numeric|#numeric>

=head3 C«tau» The number τ (6.2831...)
=item alternates: C«τ»
=item L<term|#term> L<numeric|#numeric>

=head3 C«e» Euler's number (2.7182...)
=item alternates: C«𝑒»
=item L<term|#term> L<numeric|#numeric>

=head3 C«i» The imaginary unit (√-1)
=item L<term|#term> L<numeric|#numeric>

=head3 C«Inf» Infinity
=item alternates: C«∞»
=item L<term|#term> L<numeric|#numeric>

=head2 Metaoid

The Metaoid group contains the meta operators that take an infix operator and do something
special with its result or its arguments.

=head3 C«op=» store result of infix operator on left side
=item L<infix|#infix> L<syntax|#syntax>

=head3 C«Rop» produce result of infix operator with arguments reversed
=item L<infix|#infix> L<syntax|#syntax>

=head3 C«!op» negate result of iffy infix operator on arguments
=item L<infix|#infix> L<syntax|#syntax>

=head2 Methodic

The Methodic group contains the ways a method can be called on an object.

Dotted postfix operators must be followed by a method name or a subroutine
name prefixed with `&` (possibly fully qualified), a postfix operator, a
prefix operator within `:<>`, or one of the postcircumfixes `( )` `[ ]`
`{ }` `< >` `« »`.

=head3 C«.method» method call
=item L<postfix|#postfix>

=head3 C«. method» dotty infix method call
=item L<infix|#infix>

=head3 C«.^method» meta-method call
=item L<postfix|#postfix>

=head3 C«.?method» call method if any
=item L<postfix|#postfix>

=head3 C«.*method» call all methods if any
=item L<postfix|#postfix>

=head3 C«.+method» call all methods
=item L<postfix|#postfix>

=head3 C«!method» private method call
=item L<postfix|#postfix>

=head3 C«.=method» incovant mutating method call
=item L<postfix|#postfix>

=head2 Mixoid

The Mixoid group contains the functions that allow mixing in functionality into an existing
class or instantiation.

=head3 C«does» mutating role mixin
=item L<infix|#infix>

=head3 C«but» cloning role mixin
=item L<infix|#infix>

=head2 Modifoid

The Modifoid group contains the statement modifiers to indicate conditional execution
of the preceding thunk.

=head3 L<C«if» execute thunk if condition is true|https://docs.raku.org/language/control#if>
=item L<syntax|#syntax> L<modifier|#modifier> L<thunky|#thunky>

Executes the thunk if the condition is true.

=head3 L<C«unless» execute thunk if condition is false|https://docs.raku.org/language/control#unless>
=item L<syntax|#syntax> L<modifier|#modifier> L<thunky|#thunky>

Executes the thunk if the condition is false.

=head3 L<C«with» execute thunk set if value is defined, and set topic|https://docs.raku.org/language/control#with_orwith_without>
=item L<syntax|#syntax> L<modifier|#modifier> L<thunky|#thunky>

Execute thunk if given expression produces a defined value,
sets topic inside thunk.

=head3 L<C«without» execute thunk if value is not defined, and set topic|https://docs.raku.org/language/control#with_orwith_without>
=item L<syntax|#syntax> L<modifier|#modifier> L<thunky|#thunky>

Execute thunk if given expression does B<not> produce a defined
value, sets topic inside thunk.

=head3 L<C«when» execute thunk if value smartmatches with the topic|https://docs.raku.org/language/control#when>
=item L<syntax|#syntax> L<modifier|#modifier> L<thunky|#thunky> L<topic|#topic>

Execute thunk if given expression smartmatches with the topic
(C<$_>).

=head2 Multiplicoid

The Multiplicoid group contains all infix operators that are related to multiplication in some
way.

=head3 C«div» integer divide
=item L<infix|#infix> L<integer|#integer>

=head3 C«mod» integer modulus
=item L<infix|#infix> L<integer|#integer>

=head3 C«gcd» greatest common divisor
=item L<infix|#infix> L<integer|#integer>

=head3 C«lcm» lowest common multiple
=item L<infix|#infix> L<integer|#integer>

=head3 C«*» multiply
=item alternates: C«×»
=item L<infix|#infix> L<numeric|#numeric>

=head3 C«/» divide
=item alternates: C«÷»
=item L<infix|#infix> L<numeric|#numeric>

=head3 C«**» exponentiation
=item L<infix|#infix> L<numeric|#numeric>

=head3 C«%» modulus
=item L<infix|#infix> L<numeric|#numeric>

=head3 C«%%» is divisible
=item L<infix|#infix> L<boolean|#boolean>

=head3 C«+<» integer shift left
=item L<infix|#infix> L<integer|#integer>

=head3 C«+>» integer shift right
=item L<infix|#infix> L<integer|#integer>

=head2 Normaloid

The Normaloid group contains the operators that somehow normalize the given arguments into
something else, either in time or in value.

=head3 C«+» numerify
=item L<prefix|#prefix> L<numeric|#numeric>

=head3 C«+^» numeric complement
=item L<prefix|#prefix> L<numeric|#numeric>

=head3 C«-» numeric negation
=item alternates: C«−»
=item L<prefix|#prefix> L<numeric|#numeric>

=head3 C«^» numeric from zero upto
=item L<prefix|#prefix> L<numeric|#numeric>

=head3 C«~» stringify
=item L<prefix|#prefix> L<string|#string>

=head3 C«?» boolify
=item L<prefix|#prefix> L<boolean|#boolean>

=head3 C«so» low precedence boolify
=item L<prefix|#prefix> L<boolean|#boolean>

=head3 C«!» boolean negation
=item alternates: C«?^»
=item L<prefix|#prefix> L<boolean|#boolean>

=head3 C«not» low precedence boolean negation
=item L<prefix|#prefix> L<boolean|#boolean>

=head3 C«//» is defined
=item L<prefix|#prefix> L<boolean|#boolean>

=head3 L<C«|» flatten args, slip iterable|https://docs.raku.org/syntax/%7C>
=item L<prefix|#prefix>

The meaning of prefix `|` was expanded in 6.c to be beyond the use within
signatures, to mean converting any iterable to a `Slip` (a special type of
`list` that will always be iterated).

=head3 L<C«⚛» atomic integer access|https://docs.raku.org/routine/⚛>
=item L<prefix|#prefix>

Ensures the latest update of a value in the target is produced in
multi-threaded situations.

=head2 Orderoid

The Orderoid group contains the infix operators that return an C<Order> value.

=head3 C«cmp» equivalence order comparison
=item L<infix|#infix> L<order|#order>

=head3 C«<=>» numerical order comparison
=item L<infix|#infix> L<order|#order>

=head3 C«leg» string order comparison
=item L<infix|#infix> L<order|#order>

=head2 Quantoid

The Quantoid group contains the functions that return a C<QuantHash> (a C<Set>, C<SetHash>,
C<Bag>, C<BagHash>, C<Mix> or C<MixHash> given any set or arguments.

=head3 C«∅» empty Set
=item L<term|#term> L<quanthash|#quanthash>

=head3 C«(|)» quanthash union
=item alternates: C«∪»
=item L<infix|#infix> L<quanthash|#quanthash>

=head3 C«(+)» quanthash addition
=item alternates: C«⊎»
=item L<infix|#infix> L<quanthash|#quanthash>

=head3 C«(.)» quanthash multiplication
=item alternates: C«⊍»
=item L<infix|#infix> L<quanthash|#quanthash>

=head3 C«(-)» quanthash difference
=item alternates: C«∖»
=item L<infix|#infix> L<quanthash|#quanthash>

=head3 C«(^)» quanthash symmetric difference
=item alternates: C«⊖»
=item L<infix|#infix> L<quanthash|#quanthash>

=head2 Rangoid

The Rangoid group contains the infix operators that produce a C<Range>.

=head3 C«..» range inclusive
=item L<infix|#infix> L<range|#range>

=head3 C«^..» range excluding start
=item L<infix|#infix> L<range|#range>

=head3 C«..^» range excluding end
=item L<infix|#infix> L<range|#range>

=head3 C«^..^» range exclusive
=item L<infix|#infix> L<range|#range>

=head3 C«minmax» range including min and max value
=item L<infix|#infix> L<range|#range>

=head2 Reductoid

The Reductoid group contains the functions that reduce a given set of arguments into
a single value (or produces the steps to produce that value).

=head3 C«min» reduce values to smallest value
=item L<infix|#infix> L<sub|#sub> L<method|#method>

=head3 C«max» reduce values to largest value
=item L<infix|#infix> L<sub|#sub> L<method|#method>

=head3 C«[op]» reduce using given infix operator
=item L<prefix|#prefix> L<syntax|#syntax>

=head3 C«[\op]» produce steps of reduction using given infix operator
=item L<prefix|#prefix> L<syntax|#syntax>

=head2 Replicant

The Replicant group contains the infix operators that reproduce the left value repeatedly.

=head3 C«x» string repetition
=item L<infix|#infix> L<string|#string>

=head3 C«xx» item repetition
=item L<infix|#infix> L<thunky|#thunky>

=head2 Sequoid

The Sequoid group contains the infix operators that create a smart sequence (one that
inspect the arguments and interpretes special, almost magic, meaning).

=head3 C«...» smart sequence inclusive
=item L<infix|#infix> L<sequence|#sequence>

=head3 C«^...» smart sequence excluding start
=item L<infix|#infix> L<sequence|#sequence>

=head3 C«...^» smart sequence excluding end
=item L<infix|#infix> L<sequence|#sequence>

=head3 C«^...^» smart sequence exclusive
=item L<infix|#infix> L<sequence|#sequence>

=head2 Shortoid

The Shortoid group contains the infixish macros that exhibit short-cicuiting behaviour,
where the right hand side will B<not> be evaluated if a certain condition
on the left hand side was met.

=head3 C«&&» high precedence logical AND
=item L<infix|#infix> L<macro|#macro>

=head3 C«and» low precedence logical AND
=item L<infix|#infix> L<macro|#macro>

=head3 C«||» high precedence logical OR
=item L<infix|#infix> L<macro|#macro>

=head3 C«or» low precedence logical OR
=item L<infix|#infix> L<macro|#macro>

=head3 C«^^» high precedence logical XOR
=item L<infix|#infix> L<macro|#macro>

=head3 C«xor» low precedence logical XOR
=item L<infix|#infix> L<macro|#macro>

=head3 C«//» high precedence is defined OR
=item L<infix|#infix> L<macro|#macro>

=head3 C«notandthen» Empty if first defined, else last
=item L<infix|#infix> L<macro|#macro> L<topic|#topic>

=head2 Stuboid

The Stuboid group contains the functionality that can be used to indicate that code
will need to be added at a later time (e.g. in the development cycle
of the code), or at compilation time by mixing in a role.

=head3 C«???» warn about stub code executed
=item L<term|#term> L<macro|#macro>

=head3 C«...» fail with stub code executed error
=item L<term|#term> L<macro|#macro>

=head3 C«!!!» die with stub code executed error
=item L<term|#term> L<macro|#macro>

=head2 Talkoid

The Talkoid group contains the functions that are generally used to display messages
to a user.

=head3 C«print» stringify argument(s), write to C<$*STDOUT>
=item L<sub|#sub> L<method|#method> L<string|#string>

=head3 C«say» create gist of argument(s), add newline, write to C<$*STDOUT>
=item L<sub|#sub> L<method|#method> L<string|#string>

=head3 C«put» stringify argument(s), add newline, write to C<$*STDOUT>
=item L<sub|#sub> L<method|#method> L<string|#string>

=head3 C«note» create gist of argument(s), add newline, write to C<$*STDERR>
=item L<sub|#sub> L<method|#method> L<string|#string>

=head2 Termoid

The Termoid group contains the functions that act as a term, but do B<not> produce a
constant value.

=head3 C«self» invocant in method
=item L<term|#term>

=head3 C«rand» pseudo-random Num in range 
=item L<term|#term>

=head3 C«now» C<Instant> of current time
=item L<term|#term>

=head3 C«time» POSIX time in seconds
=item L<term|#term>

=head3 C«nano» POSIX time in nano seconds
=item L<term|#term>

=head2 Throwoid

The Throwoid group contains the functions that somehow interrupt the normal flow of execution
either very noticeably, or very much under the hood.

=head3 C«die» halt execution, or be caught by C<CATCH>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«warn» show error message, or be caught by C<CONTROL>
=item L<sub|#sub>

=head3 C«fail» return from Routine with Failure, or be caught by C<CATCH>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«return» return from Routine with given value, or be caught by C<CONTROL>
=item L<sub|#sub> L<method|#method> L<interrupt|#interrupt>

=head3 C«next» proceed with next iteration, or be caught by C<CONTROL>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«redo» restart current iteration, or be caught by C<CONTROL>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«last» stop iterating, or be caught by C<CONTROL>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«proceed» continue with next when/default, or be caught by C<CONTROL>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«succeed» continue after last when/default, or be caught by C<CONTROL>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«done» call "done" callback on taps, or be caught by C<CONTROL>
=item L<sub|#sub> L<interrupt|#interrupt>

=head3 C«emit» emit given value to active supply, or be caught by C<CONTROL>
=item L<sub|#sub> L<method|#method>

=head3 C«take» produce a value in a C<gather> sequence, or be caught by C<CONTROL>
=item L<sub|#sub> L<method|#method>

=head2 Topicoid

The Topicoid group contains the functions that topicalize (set C<$_>) in some situations.

=head3 C«given» topicalize argument for scope
=item L<syntax|#syntax> L<topic|#topic>

=head3 C«when» if smartmatched topicalize argument for scope
=item L<syntax|#syntax>

=head3 C«andthen topic» topicalize left for right thunk
=item L<infix|#infix> L<thunky|#thunky>

=head3 C«orelse topic» low precedence if defined OR, topicalizing left for right thunk
=item L<infix|#infix> L<thunky|#thunky>

=head2 Tweakoid

The Tweakoid group contains all dynamic variables that 
scope (looking up the callstack), or in the C<GLOBAL::> or C<PROCESS::>
stashes.  All of these variable can be shadowed by lexically defined
variables with the same name.

=head3 L<C«$*IN» filehandle for STDIN (standard input)|https://docs.raku.org/language/variables#Special_filehandles:_STDIN,_STDOUT_and_STDERR>
=item L<variable|#variable> L<dynamic|#dynamic>

The file handle for reading input, e.g. with C<.lines> or C<.slurp>.

=head3 L<C«$*OUT» filehandle for STDOUT (standard output)|https://docs.raku.org/language/variables#Special_filehandles:_STDIN,_STDOUT_and_STDERR>
=item L<variable|#variable> L<dynamic|#dynamic>

The file handle for writing standard output to, e.g. with C<say>.

=head3 L<C«$*ERR» filehandle for STDERR (error output)|https://docs.raku.org/language/variables#Special_filehandles:_STDIN,_STDOUT_and_STDERR>
=item L<variable|#variable> L<dynamic|#dynamic>

The file handle for writing error output to, e.g. with C<note>.

=head3 L<C«$*CWD» what should be considered the default directory|https://docs.raku.org/language/variables#$*CWD>
=item L<variable|#variable> L<dynamic|#dynamic>

An C<IO::Path> object that contains the directory that should be
considered to be the current working directory.  Initialized with the
OS's current directory at startup.

=head3 L<C«$*DEFAULT-READ-ELEMS» the number of bytes to read by default in binary mode|https://docs.raku.org/language/variables#$*DEFAULT-READ-ELEMS>
=item L<constant|#constant> L<dynamic|#dynamic> L<integer|#integer>

Indicates the default number of bytes to read with
C<IO::Handle.read>.

=head3 L<C«$*COLLATION» how to collate (unicode sort)|https://docs.raku.org/language/variables#$*COLLATION>
=item L<constant|#constant> L<dynamic|#dynamic>

A C<Collation> object that describes how the C<collate> function
and the C<coll> infix operator will handle unicode graphemes.

=head3 L<C«$*RAT-OVERFLOW» action to perform on C<Rat> overflow|https://docs.raku.org/language/variables#$*RAT-OVERFLOW>
=item L<variable|#variable> L<dynamic|#dynamic>

Contains a class to indicate what to do when a C<Rat> object
overflows.  By default, it will convert the value to a lossy
C<Num> value.

=head3 L<C«$*TOLERANCE» how much a value may differ with C<=~=>|https://docs.raku.org/language/variables#$*TOLERANCE>
=item L<variable|#variable> L<dynamic|#dynamic>

The difference a numeric value may have from a target value in
order to be assumed the same with the infix C<=~=> operator.

=head3 L<C«$*TZ» timezone in seconds|https://docs.raku.org/language/variables#$*TZ>
=item L<variable|#variable> L<dynamic|#dynamic> L<integer|#integer>

The current timezone offset in seconds.  B<0> implies the user
is running on C<UTC>.  Initialized with the OS's time offset at
startup.

=comment PLEASE DON'T CHANGE ANYTHING ABOVE THIS LINE

=head1 RESOURCE FILES

This distribution keeps all of the text files as resources, to be read when
they are needed (as opposed to keeping them all in the binary).

The C<resources> directory contains a C<groups> directory that contains all
of the information for each group in a separat file.

=head2 Group file format

The format of the group file is very simple line based.

All entries are separated by an empty line.  The first line contains the
names of the element separated by the string C< | > (space, pipe, space).
There must be at least one name.  The first name becomes the C<name> of
the element, the others become C<aliases>.

The second line contains theo tags as space separated words.  There must
be at least a single tag.

The third line contains the tagline of the element.

The fourth line (if not empty) contains the URL for more information,

Any other lines until an empty line, contains the description of the
element.  It may contain RakuDoc markup codes.

If the first line is empty, then the rest of the file contains the
description of the group (so you can recognize the description as the
text after the first 2 empty lines).  It may contain RakuDoc markup
codes.  If there are no 2 empty lines, then there is B<no> description
(yet) for the group.

=head2 Tag file format

The whole text of a tag file becomes the C<.description> of the tag
with the name of the file.

=head1 AUTHOR

Elizabeth Mattijsen <liz@raku.rocks>

Source can be located at: https://github.com/lizmat/Raku-Elements .
Comments and Pull Requests are welcome.

If you like this module, or what I’m doing more generally, committing to a
L<small sponsorship|https://github.com/sponsors/lizmat/>  would mean a great
deal to me!

=head1 COPYRIGHT AND LICENSE

Copyright 2025 Elizabeth Mattijsen

This library is free software; you can redistribute it and/or modify it under the Artistic License 2.0.

=end pod

# vim: expandtab shiftwidth=4
